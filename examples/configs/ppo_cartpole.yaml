# Declarative experiment: run with `decisionrl run examples/configs/ppo_cartpole.yaml`
env: CartPole
algo:
  name: ppo
  n_steps: 1024
  batch_size: 64
  n_epochs: 10
seed: 0
total_steps: 50000
eval_episodes: 20
